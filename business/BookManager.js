class BookManager {

	getBook(bookID) {
		// Appelle la méthode getBook(bookID) de l'orm
		// Retourne le livre
	}

	updateBook(book) {
		// Appelle la méthode updateBook(bookID) de l'orm
	}

	getAvailableBooks() {
		// Appelle la méthode getAllBooks() de l'orm
		// filtre les livres qui sont empruntés
		// retourne la liste des livres non empruntés
	}

	getAllBooks() {
		// Appelle la méthode getAllBooks() de l'orm
		// retourne la liste des livres
	}
}
