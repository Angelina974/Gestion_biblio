class UserView {
	constructor(userService) {
		// lorsqu'on créer une instance de UserView on va lui faire passer le paramètres : userService,
		// qui cherche à ajouter / supprimer les utilisateur dans l'application
		// On enregistre cet objet dans la propriété this.userService pour l'utiliser plus tard dans cette classe.
	}

	displayUserDetails(userID) {
		// On va récupérer les infos de l'utilisateur avec le paramètre userId puis appelé la fonction display pour l'afficher
	}

	display(user) {
		// on affiche
	}
}
