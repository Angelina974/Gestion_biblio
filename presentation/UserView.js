class UserView {

	displayUserDetails(userID) {
		// Appelle la méthode getUser(userId) de UserManager
		// Appelle la méthode display(user) pour l'afficher l'utilisateur récupéré
	}

	display(user) {
		// Affiche
	}

	updateUserDetails(userID) {
		// Appelle la méthode updateUser(user) pour mettre à jour l'utilisateur récupéré
	}

}
