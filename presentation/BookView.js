class BookView {

	displayAvailableBooks() {
		// Récupère les livres disponibles via la méthode getAvailableBooks() de BookManager.
		// Affiche les livres récupérés.
	}

	displayAllBooks() {
		// Récupère la liste des livres via la méthode getAllBooks() de BookManager.
		// Affiche la liste des livres récupérée
	}

	displayBook(book) {
		// Récupère le livre via la méthode getBook(bookID) de BookManager.
		// Affiche le livre récupéré
	}

	updateBook(bookId) {
		// Met à jour le livre via la méthode updateBook(bookId) de BookManager.
	}

}
