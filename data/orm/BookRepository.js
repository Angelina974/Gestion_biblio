class BookRepository {

	getBook(bookID) {
		// Vérifie si le livre en question existe dans la base de données
		// Si le livre n'existe pas la méthode renvoie un livre vide
		// Si le livre existe, le livre en question est renvoyé
	}

	getAllBooks() {
		// Retourne la liste de tous les livres de la base de données
	}

	updateBook(book) {
		// Vérifie si le livre en question existe dans la base de données
		// Si le livre existe, il est mis à jour dans la base de données
	}

}
